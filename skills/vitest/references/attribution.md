# Attribution

This skill is authored for the local `agent-skills` repository and follows the lightweight structure of the sibling `web-debug` skill.

It is intended to complement official Vitest documentation and common ecosystem patterns from Vite, Vue/Nuxt, React Testing Library, and Vue Test Utils projects. It does not vendor upstream documentation.

